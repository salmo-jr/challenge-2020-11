import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        backgroundColor: '#222'
    },
    searchField: {
        backgroundColor: '#ccc',
        borderColor: '#000',
        margin: 5,
        padding: 5
    }
});